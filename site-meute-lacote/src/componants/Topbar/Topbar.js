import React from 'react';

import classes from './Topbar.module.css';

const topbar = () => (
    <div className={classes.Topbar}>
        <div className={classes.Title}>La meute St Francois</div>
    </div>
)

export default topbar;